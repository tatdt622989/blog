<% if (site.categories.length){ %>
<div class="widget tag category-summary">
  <h3 class="title"><%= __('categories') %></h3>
  <ul class="entry">
  <% site.categories.sort('name').each(function(item){ %>
    <li><a href="<%- url_for(item.path) %>"><%= item.name %></a><small><%= item.posts.length %></small></li>
  <% }); %>
  </ul>
  <a class="widget-directory-link" href="<%- url_for('categories/') %>"><%= __('all_categories') %></a>
</div>
<% } %>
